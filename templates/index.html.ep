% layout 'default';
% title 'Welcome';

% if ( $error ) {

% }
% if ( $screen_name && $profile  ) {
Yahooの構文解析apiを使う プロフィールの名詞を抜き出す
<p>my name is <%= $screen_name %></p>
<p>profile is <%= $profile %></p>
<p>Your Ishiki is <%= $ishiki %></p>
% for my $tweet (@{$remarks}) {
<p><%= Mojo::ByteStream->new($tweet) %></p>
% }
% }
